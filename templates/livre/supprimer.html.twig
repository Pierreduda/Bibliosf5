{% extends 'base.html.twig' %}

{% block title %}LivreController
{% endblock %}

{% block body %}
	<h1>Confirmation de la suppression du livre n°
		{{ livre.id }}</h1>

	<ul class="list-group">
		<li class="list-group">
			<strong>Titre:
			</strong>
			{{livre.titre}}
		</li>
		<li class="list-group">
			<strong>Auteur:
			</strong>
			{{livre.auteur}}
		</li>
	</ul>


	<div class="row mt-3">
		<form method="post" >
			<button class="btn btn-primary" type="submit">Confirmer</button>
		</form>
		<a href="{{ path("livre") }}" class="btn btn-danger">annuler</a>
	</div>
{% endblock %}
